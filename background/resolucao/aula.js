const body = document.querySelector('body');
const btns = document.querySelectorAll('button');

const coloracoes = ['red','yellow', 'blue', 'green']



  btns[0].addEventListener('click', function(evento){
    console.log(evento)
  })
